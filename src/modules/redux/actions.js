export const loadUsers = (users) => ({
  type: 'LOAD_USERS',
  users,
});

export const loadPhotos = (photos) => ({
  type: 'LOAD_PHOTOS',
  photos,
});

export const loadPosts = (posts) => ({
  type: 'LOAD_POSTS',
  posts,
});
